const environments = {
    allowWebServer: true,
    httpPort: 3000,
    dataFrom: 'url', // from file or url
    dataFile: './data/coins.json',
    dataUrl: 'http://localhost:3000/example',
};

module.exports = environments;
