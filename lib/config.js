const environments = {
    allowWebServer: true,
    httpPort: 3000,
    dataFrom: 'file', // from file or url
    dataFile: './data/coins.json',
    dataUrl: null,
};

module.exports = environments;
